name: Jekyll site CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Test HTML
      run: |
        curl https://htmltest.wjdp.uk | bash
        bin/htmltest

    
